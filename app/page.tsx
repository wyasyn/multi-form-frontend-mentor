import MainForm from "@/components/Form";

export default function page() {
    return (
        <main className=" min-h-dvh lg:flex lg:items-center lg:justify-center ">
            <MainForm />
        </main>
    );
}
